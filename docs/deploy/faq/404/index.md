# 页面 404

部署后访问页面出现 404，常见原因：

- **仓库未启用 Pages**：Settings → Pages 未配置发布源
- **分支 / 目录选错**：发布源不是 `main` 分支根目录（或 `/docs`）
- **文件名不符**：首页需为 `index.html`，注意大小写
- **用户页仓库命名**：用户页仓库必须为 `<用户名>.github.io`，项目页为 `<用户名>.github.io/<仓库名>`

排查时先在仓库 Settings → Pages 查看构建状态与发布 URL。
